import AddProyectoClient from "@/components/add-proyecto-client";
import { Suspense } from "react";

export default function AddProyecto() {
  return(
    <Suspense><AddProyectoClient/></Suspense>
  )
}